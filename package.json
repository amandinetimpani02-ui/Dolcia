{
  "name": "dolcia",
  "version": "21.40.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "scripts": {
    "start": "vercel dev",
    "test": "node --test test/*.test.mjs",
    "check": "npm test && node --check app.js && node --check server/recommendations.js && node --check server/geo-eligibility.js",
    "deploy": "npm run check && vercel --prod"
  }
}
