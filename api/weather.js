export default async function handler(req,res){try{const key=process.env.OPENWEATHER_KEY;if(!key)return res.status(500).json({error:"OPENWEATHER_KEY missing"});const{lat,lng}=req.query;const r=await fetch(`https://api.openweathermap.org/data/2.5/weather?lat=${lat}&lon=${lng}&appid=${key}&units=metric&lang=fr`);res.status(200).json(await r.json())}catch(e){res.status(500).json({error:e.message})}}