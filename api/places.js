export default async function handler(req,res){try{const{lat,lng,radius=10000,type,keyword,mode}=req.query;const key=process.env.GOOGLE_KEY;if(!key)return res.status(500).json({error:"GOOGLE_KEY missing"});let url;if(mode==="text"||keyword){url=`https://maps.googleapis.com/maps/api/place/textsearch/json?query=${encodeURIComponent(keyword||"activité")}&location=${lat},${lng}&radius=${radius}&key=${key}&language=fr`;}else{url=`https://maps.googleapis.com/maps/api/place/nearbysearch/json?location=${lat},${lng}&radius=${radius}&type=${encodeURIComponent(type||"tourist_attraction")}&key=${key}&language=fr`;}const r=await fetch(url);res.status(200).json(await r.json())}catch(e){res.status(500).json({error:e.message})}}