self.addEventListener('push',event=>{let data={title:'Dolcia Partenaires',body:'Une mise à jour vous attend.',url:'/pro.html'};try{data={...data,...event.data.json()}}catch(_){}event.waitUntil(self.registration.showNotification(data.title,{body:data.body,data:{url:data.url},badge:'/assets/hero-le-touquet.png'}))});self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.openWindow(event.notification.data?.url||'/pro.html'))});
